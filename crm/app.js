const seed={cases:[{id:'CASE-1001',account:'Northwind Traders',subject:'Invoice reconciliation delay',severity:'Medium',status:'In progress',owner:'Finance Operations'},{id:'CASE-1002',account:'Contoso Retail',subject:'Warehouse integration timeout',severity:'High',status:'Escalated',owner:'Integration Team'},{id:'CASE-1003',account:'Northwind Traders',subject:'Order confirmation emails delayed',severity:'Low',status:'Resolved',owner:'Messaging Team'}],tasks:[{id:'TASK-2001',title:'Validate order queue health',caseId:'CASE-1002',owner:'Operations',due:'Today',status:'In progress'},{id:'TASK-2002',title:'Confirm customer update',caseId:'CASE-1001',owner:'Account Team',due:'Today',status:'Not started'},{id:'TASK-2003',title:'Publish resolution summary',caseId:'CASE-1003',owner:'Service Manager',due:'Sep 27',status:'Complete'}],accounts:[{name:'Northwind Traders',industry:'Manufacturing',health:'At risk',open:2,owner:'Alex Morgan'},{name:'Contoso Retail',industry:'Retail',health:'Watch',open:1,owner:'Jamie Lee'}],knowledge:[{title:'Order processing delays - API queue saturation',source:'Historical case CASE-0921',type:'Resolution',text:'Clear queued jobs, validate downstream API capacity, and monitor for 30 minutes.'},{title:'Order operations incident playbook',source:'Approved playbook',type:'Procedure',text:'Notify the service owner, open an incident bridge, and confirm recovery with a test order.'},{title:'Leadership issue update template',source:'Approved template',type:'Communication',text:'State the impact, known facts, current action, owner, and next checkpoint.'}]};
let data=JSON.parse(localStorage.getItem('demo-crm')||'null')||seed;const $=s=>document.querySelector(s);const esc=s=>String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const sev=s=>`<span class="sev ${s.toLowerCase()}">${esc(s)}</span>`;function save(){localStorage.setItem('demo-crm',JSON.stringify(data));render()}
function render(){const open=data.cases.filter(c=>c.status!=='Resolved');$('#open-cases').textContent=open.length;$('#high-cases').textContent=data.cases.filter(c=>c.severity==='High').length;$('#due-tasks').textContent=data.tasks.filter(t=>t.due==='Today'&&t.status!=='Complete').length;$('#case-count').textContent=data.cases.length;$('#task-count').textContent=data.tasks.length;$('#priority-cases').innerHTML=data.cases.slice(0,4).map(c=>`<div class="case"><div><strong>${esc(c.subject)}</strong><div class="meta">${esc(c.id)} · ${esc(c.account)} · ${esc(c.status)} · ${esc(c.owner)}</div></div>${sev(c.severity)}</div>`).join('');$('#all-cases').innerHTML=`<table class="table"><thead><tr><th>Case</th><th>Account</th><th>Subject</th><th>Severity</th><th>Status</th><th>Owner</th></tr></thead><tbody>${data.cases.map(c=>`<tr><td><b>${esc(c.id)}</b></td><td>${esc(c.account)}</td><td>${esc(c.subject)}</td><td>${sev(c.severity)}</td><td>${esc(c.status)}</td><td>${esc(c.owner)}</td></tr>`).join('')}</tbody></table>`;$('#all-accounts').innerHTML=data.accounts.map(a=>`<div class="account"><h3>${esc(a.name)}</h3><p>${esc(a.industry)} · Owner: ${esc(a.owner)}</p><p><b>${a.open}</b> open cases · ${sev(a.health)}</p></div>`).join('');$('#all-tasks').innerHTML=`<table class="table"><thead><tr><th>Task</th><th>Case</th><th>Owner</th><th>Due</th><th>Status</th></tr></thead><tbody>${data.tasks.map(t=>`<tr><td><b>${esc(t.title)}</b></td><td>${esc(t.caseId)}</td><td>${esc(t.owner)}</td><td>${esc(t.due)}</td><td>${esc(t.status)}</td></tr>`).join('')}</tbody></table>`;renderKnowledge($('#knowledge-search')?.value||'')}
function renderKnowledge(q=''){const list=data.knowledge.filter(k=>(k.title+k.text+k.source).toLowerCase().includes(q.toLowerCase()));$('#knowledge-list').innerHTML=list.map(k=>`<div class="knowledge"><strong>${esc(k.title)}</strong><div class="meta">${esc(k.type)} · ${esc(k.source)}</div><p>${esc(k.text)}</p></div>`).join('')||'<p>No matching knowledge found.</p>'}
function show(view){document.querySelectorAll('.view').forEach(v=>v.classList.add('hidden'));$(`#view-${view}`).classList.remove('hidden');document.querySelectorAll('.nav').forEach(n=>n.classList.toggle('active',n.dataset.view===view));$('#title').textContent={home:'Issue command center',cases:'Cases',accounts:'Accounts',tasks:'Tasks',knowledge:'Knowledge'}[view]}
document.addEventListener('click',e=>{const nav=e.target.closest('[data-view]');if(nav)show(nav.dataset.view);if(e.target.id==='new-case')$('#modal').classList.remove('hidden');if(['close','cancel'].includes(e.target.id))$('#modal').classList.add('hidden');if(e.target.id==='copy-prompt'){navigator.clipboard?.writeText('Assess the Northwind order-processing issue, find similar cases, and create an action plan.');$('#copy-status').textContent=' Copied';setTimeout(()=>$('#copy-status').textContent='',1600)}});$('#case-search').addEventListener('input',e=>{const q=e.target.value.toLowerCase();document.querySelectorAll('#all-cases tbody tr').forEach(r=>r.style.display=r.textContent.toLowerCase().includes(q)?'':'none')});$('#knowledge-search').addEventListener('input',e=>renderKnowledge(e.target.value));$('#case-form').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.target),id=`CASE-${1004+data.cases.length-3}`;data.cases.unshift({id,account:f.get('account'),subject:f.get('subject'),severity:f.get('severity'),status:'New',owner:'Account Team'});data.tasks.unshift({id:`TASK-${2004+data.tasks.length-3}`,title:'Confirm issue assessment and next step',caseId:id,owner:'Account Team',due:'Today',status:'Not started'});save();$('#modal').classList.add('hidden');show('cases');$('#toast').textContent=`${id} created in demo CRM`;$('#toast').style.display='block';setTimeout(()=>$('#toast').style.display='none',2500);e.target.reset()});render();
